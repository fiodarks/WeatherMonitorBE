services:
  - type: web
    name: weather-monitor
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: weather-monitor-db
          property: connectionString

databases:
  - name: weather-monitor-db
